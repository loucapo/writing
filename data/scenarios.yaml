# i need to know -
# what db-role (gw, lms1, etc)
# what scenario name
# and i return
# the db (schema) name and a filename
---
default:
  eco:
    schema: eco
    file: stem-eco-RC11.sql
  moodle:
    schema: moodle
    #file: moodle-2.8.6-configured-for-RC10.sql
    file: moodle3-base-preeco-c.sql
  gateway:
    schema: mnv_gateway
    # file: gateway-current.sql
    file: new-new-gateway.sql
