version: "2"
services:
  wk_api:
    image: wk_api
    volumes:
      - ./../app:/home/current/app:rw
    command: npm start #top -b #
    env_file:
      - ./.env
    environment:
      DEBUG: "true"
      NODE_ENV: development
      APPLICATION_NAME: wk_api
    ports:
      - "3000:3000"
