{
  "title": "Course",
  "description": "Course",
  "type": "object",
  "required": [
    "id",
    "title",
    "abbreviation",
    "courseId",
    "instructorId"
  ],
  "properties": {
    "id": {
      "description": "The in system unique identifier for a course",
      "type": "string",
      "format": "uuid"
    },
    "title": {
      "description": "Course title",
      "type": "string"
    },
    "abbreviation": {
      "description": "The abbreviation of the course title",
      "type": "string"
    },
    "courseId": {
      "description": "The unique foreign key identifier for a course found in another system",
      "type": "string"
    },
    "instructorId": {
      "description": "Foreign key for the instructor who's course this is",
      "type": "string",
      "format": "uuid"
    }
  }
}