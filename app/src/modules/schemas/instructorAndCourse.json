{
  "title": "instructorAndCourse",
  "description": "instructorAndCourse for lti launch",
  "type": "object",
  "required": [
    "firstName",
    "lastName",
    "IAM",
    "userType",
    "userRoles",
    "title",
    "abbreviation",
    "courseId",
    "instructorId"
  ],
  "properties": {
    "firstName": {
      "type": "string"
    },
    "lastName": {
      "type": "string"
    },
    "email": {
      "type": "string",
      "x-faker": "email"
    },
    "IAM": {
      "description": "The unique foreign key identifier for a user found in our IAM",
      "type": "string"
    },
    "userType": {
      "description": "Distinguisher on user type.  Currently [Instructor, student]",
      "type": "string"
    },
    "title": {
      "description": "Course title",
      "type": "string"
    },
    "abbreviation": {
      "description": "The abbreviation of the course title",
      "type": "string"
    },
    "courseId": {
      "description": "The unique foreign key identifier for a course found in another system",
      "type": "string"
    },
    "instructorId": {
      "description": "Foreign key for the instructor who's course this is",
      "type": "string",
      "format": "uuid"
    }
  }
}