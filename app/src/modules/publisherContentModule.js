
const pubContent = [
  {
    id: '0792f178-d834-4427-a43f-a27dd2c58ca1',
    type: 'QF',
    contentType: 'fragment',
    content: `Description: A fragment is an incomplete sentence.
     As you might suspect, it is also an incomplete expression of a thought.
    Fragments come in two varieties:
    1. A dependent clause, which contains a subject and a verb but fails to express a complete thought
    2. A group of words that lacks either a subject or a verb`
  }
];

// Reducer
export default (state = pubContent) => {
  return state;
};

// Action
// actions will be available when this stuff is dynamic.
